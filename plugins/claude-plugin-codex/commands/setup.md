---
description: Check whether Claude CLI is installed and usable from Codex.
argument-hint: ""
allowed-tools: [Bash(node:*), Bash(claude:*)]
---

# Claude Setup

Call the `claude_setup` MCP tool.

Return the tool output to the user. If Claude is not ready, include the setup guidance exactly as printed.
